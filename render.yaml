services:
  - type: web
    name: wynajem-busow-jaroslaw
    runtime: static
    rootDir: react-app
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
